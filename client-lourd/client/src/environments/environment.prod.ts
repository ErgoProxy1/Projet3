import {FIREBASE_CONFIG, IEnvironment} from './IEnvironment';

export const environment: IEnvironment = {
  production: true,
  socketUrl: 'https://projet3-111.herokuapp.com/',
  firebaseConfig: FIREBASE_CONFIG
};
